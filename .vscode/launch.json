{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug main.exe (CodeLLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/main.exe",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Build with clang++"
    }
  ]
}