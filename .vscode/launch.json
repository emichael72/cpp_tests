{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug main (CodeLLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/bin/cpp_tests",
      "args": ["-a"],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Build with make",
    }
  ]
}